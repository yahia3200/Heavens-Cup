# Configure nginx to host the static files and listen on port 80
server {
    listen 80;
    server_name localhost;
    location / {
        try_files $uri /index.html;
        root /var/www/html;
    }
}